#include <iostream>
#include <cmath>
using namespace std;
int main() {
    float p, r, t, a, ci;
    cin >> p >> r >> t;
    a = p * pow(1 + r / 100, t);
    ci = a - p;
    cout << "Compound Interest = " << ci;
    return 0;
}
